package teststudy.com.joypupil.controller;

public class DemoThread implements Runnable {
	
	private boolean running = true;

	@Override
	public void run() {
		//前段逻辑
		//循环写入逻辑（基本中断的时候都在这里），这里加入判断,每次循环外层多加一个判断
		while(running){
			
		}
		//后段逻辑，判断他写入了多少，是否已经写完了，然后存入库中
	}
	
	public void shutdown(){
		this.running = false;
	}

}
